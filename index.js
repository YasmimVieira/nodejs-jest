const somaHorasExtras = (salario, valorHoraExtra) => salario + valorHoraExtra;

const calculaDescontos = (salario, descontos) => salario - descontos;

export {
  somaHorasExtras,
  calculaDescontos,
};
